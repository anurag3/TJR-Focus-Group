package anuragdesai.library.kneereplacement;

import android.graphics.drawable.Drawable;
import android.graphics.drawable.ScaleDrawable;
import android.support.v4.content.ContextCompat;
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;
import android.view.Menu;
import android.view.MenuItem;
import android.widget.Button;

public class TransparentButtonText extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_transparent_button_text);
//
//        Button aButton = (Button) findViewById(R.id.transparent_a);
//        Drawable drawable =ContextCompat.getDrawable(this, R.drawable.atransparent);
//        drawable.setBounds(0, 0, (int) (drawable.getIntrinsicWidth() * 0.5),
//                (int) (drawable.getIntrinsicHeight() * 0.5));
//        ScaleDrawable sd = new ScaleDrawable(drawable, 0, (float) 0.1, (float) 0.1);
//        aButton.setCompoundDrawablesWithIntrinsicBounds(sd.getDrawable(), null, null, null);

    }
}
